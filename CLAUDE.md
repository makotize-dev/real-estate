# CLAUDE.md — 宅建士試験対策プロジェクト

## プロジェクト概要

**目的**：税理士として宅建士（宅地建物取引士）資格を取得し、相続・不動産コンサルティング業務への展開を図る。
**受験予定**：2026年10月18日（日）　**ターゲット**：38点以上（合格ラインは例年37点前後）
**ユーザー**：税理士。民法・税法の基礎知識と法律文書読解力が強み。確定申告期（1〜3月）は学習困難で、4〜9月が主戦場。

科目別の出題数・目標点・強み弱み・週配分・ラウンド構成は `memory/study_plan.md`。ツールの現状とTODOは `memory/project_progress.md`。

---

## リポジトリ構成（要点のみ。網羅的な一覧は `ls` で確認する）

- `study_notes/{宅建業法|権利関係|法令制限|税・その他}.md` — 個人用リファレンス。Claude がセッション末に追記する
- `study_log/index.md` — 進捗サマリー（毎セッション更新）／`YYYY-MM-DD.md` — 日次ログ（雛形は `_template.md`）
- `past_exams/` — 実物の年度別過去問。**PDFはgitignore・.mdのみ追跡**。`README.md`（運用ルール・**活動開始時に必読**）と `過去問演習記録.md`（結果・推移・誤答論点の恒久記録）
- `laws.md` — **一次資料の取得手順とURLテンプレート**。条文・PDF・外部資料を取りに行く前に必ず開く（漢字URLエンコードを記憶で作らない／**日本語PDFはUTF-8でファイルに書き出してから読む**／WebFetch拒否時は生XML）
- `exam_v6.html` 想起トレーナー／`roadmap.html` 螺旋型ロードマップ（**計画の一次**）／`study_notes.html`・`study_log.html` ビューワー
- `guide-to-be-certified/` — 税理士向けダブルライセンスガイド6章（`takken_chapter1〜6_detailed.md` ＋ `takken_guide_outline.md`）
- PWA：`manifest.json` / `sw.js` / `icon.svg`。公開先 https://makotize-dev.github.io/real-estate/

---

## 鉄則（毎セッション・例外なし）

過去のセッションで繰り返し破ってきた行動を、ここに絶対ルールとして固定する。これは毎セッション必ず読まれる。違反したら、その場で直すだけでなく、原因を `study_log` のその日のログに記録する。**ルールはこの4項目を「書き換えて鋭くする」。日付つきの事件記録をここに追記しない**（事件の経緯は `study_log` に残す）。

- **A. 一次確認を先に。** 条文番号・条文文言・citation・改正の数字を書く前に、e-Gov／一次資料を取得して確認する。説明文を1文字でも書く前に取得する。未確認のものは「未確認」と文中で明示するか、書かない（**条文に限らない。フックの発火・テストの結果・画面の表示など、自分に観測手段のない事実も断定しない**）。要約版を原文と思い込まない。語句の「ある／ない」は逐語原文で確認してから断定する。**表・リスト・連続項目を書くときは全要素を逐語で確認してから書く**（一部しか確認していないなら、確認範囲を明示し未確認部分は「未確認」と書く。「いろは順で続くはず」のような推測で残りを埋めない）。**「標準的説明」「判例の整理」「体系書での通説」として書くときは、最初から「（逐語未確認）」とフラグを立てる**（指摘されてから出典を確認するのでは遅い）。
- **B. 評価語を使わない。** 「良い」「いい」「鋭い」「正確」「素晴らしい」「面白い」「見事」等の形容詞でユーザーや発言を褒めない。正誤判定（「正解です」「不正解です」）は省略せず行う。
- **C. 捏造しない。** ユーザーの発言を作り出さない。発言の帰属を推測で決めない。**検査可能な形**：①**問いを出す回は、問いをメッセージの最後に置く**（問いの後ろに解答・判定・解説・次の問いを一切書かない。判定は必ず次の回で行う）。②会話ロール記号（`user` / `assistant` / `human`）を行頭に出力しない。③**出題中は無意味なツール呼び出し（`echo` 等の no-op Bash）をしない**——ツール継続でターンが延び、その中でユーザーの解答を捏造して自己採点する引き金になる。④**採点は「1解答＝1メッセージ」**。複数肢をまとめて出題したら、必要な条文を**すべて取得してから**採点を1通に集約する。①〜④は Stop フック `.claude/hooks/no-fabricated-turn.js` が機械的に検出してブロックする（設定：`.claude/settings.json`）。フックはセッション途中で作成すると設定ウォッチャーが読まず発火しないので、**新しいセッションの開始時に有効か確認する**。
- **D. 確認を浅く打ち切らない／勝手な判断もしない。** 調査は十分な範囲で行う（例：git 履歴は `--all` で見る）。**ノート・計画・README は見出しでなく中身を読んでから判断する（見出しだけで既習/未習・既定/未定を判定しない）**。計画・ルールで既に決まっていることをユーザーに委ねる確認をしない（自分で判断・実行する）。逆に決まっていないこと（セッション終了・論点切替等）を勝手に決めない。最低ラインを達成しても、終了タイミングはユーザーの判断。

詳細・背景は `memory/feedback_source_accuracy.md`（A）と `memory/feedback_teaching_style.md`（B〜D）を参照。

---

## セッション開始時の確認手順（必須）

新しい会話が始まったら（同じスレッドでも別日の学習開始や長時間中断後の再開を含む）、**S1 →宣言→ S2** の順で行う。**S1を全部終えてから話しかけるのではない。**

**S1＝宣言に必要な4点（これ以上は読まない）**

1. 上の「鉄則」4項目を読み直し、`memory/feedback_source_accuracy.md`・`memory/feedback_teaching_style.md` を Read する（自動では読まれない）。あわせて **Stop フックが有効か確認する**（`.claude/settings.json` に `no-fabricated-turn.js`・`context-length-warn.js`）
2. `study_log/index.md` を**2ブロックだけ**読む＝〔冒頭〜「現在のフェーズ」〕と〔**当週の日別計画**〕。過去週の日別計画・週次進捗・カバレッジ・セッション一覧は読まない（**範囲を切らないと569行を全部読むことになる**）
3. `study_log/宿題.md` を読む → **今日が「次回出題日」の想起項目**（1枠8件・1セッション6件の上限は同ファイル 2の3）と**トリガー待ち**の発火条件
4. 直近の `study_log/YYYY-MM-DD.md` と `git log --all --oneline`（ローカル `main` がリモートより遅れていないか）

**→ ここで宣言する。**〔今日の枠＝科目・時間・**学習枠か保守枠か**〕〔今日出す想起の件数〕〔S1で見つけた異常〕を述べ、ユーザーの意図（何を学びたいか・どこから始めるか）を確認してから S2 へ進む。**1〜4を無言で走らせない。**

**S2＝枠が決まってから、その枠に関係するものだけ読む**

5. `roadmap.html` の当週の枠（**計画の一次**。`index.md` の日別計画は二次）／`memory/study_plan.md`（週配分）／`memory/project_progress.md`
6. **新しい分野・新しい問・過去問に入るなら** `past_exams/過去問演習記録.md`（実物を解く回は `past_exams/README.md` も）
7. **その科目のブロック学習に入るなら** `study_log/未収録一覧.md`

**ソクラテス式で進める**：いきなり解説や構造図を出さない。まず質問を投げて考えさせ、回答を受けてから補足・修正・深掘りする。

---

## 行動の直前に発火するチェック（必須）

**鉄則A〜Dは常時。この表は「行動ごと」。**開始時に読むだけでは効かないと実測されたルールを、**行動のトリガー**の形に置き直したもの（2026-08-16＝鉄則A原則13は開始手順で到達していたのに発火せず／`feedback_studynotes_structure.md` 原則5は要約つきで名指しされていたのに2回連続で発火せず）。**左の行動をする直前に、右を実行する。**

| この行動をする直前に | 実行すること |
|---|---|
| **件数・行数・全件の一覧・表を書く** | 数えてから書く。目視の概算を書かない。**条文に限らず、自分の管理ファイルの行数も対象**（鉄則A原則13）|
| **問いを出す** | ①その問いが参照を求める条文を、**その回に逐語で提示したか**（**要約1行に畳んだものは提示に数えない**）②`memory/feedback_teaching_style.md` の「問いを立てる前の構造検証」6項目 |
| **一次資料を取りに行く**（条文・PDF・外部サイト・初めての法令）| **まず `laws/{法令名}.md` を見る**（逐語キャッシュ。あればそれを使う＝取りに行かない）。**無ければ取得し、その回のうちに `laws/` へ逐語で追記する**（要約を混ぜない・表は表のまま・`asof=2026-04-01` で統一）。取得の手順は `laws.md` の「補足：…（Claude側の運用）」以下を開く。**URLテンプレートだけの話ではない**——PDFの読み方・WebFetch拒否時の生XML・別表/附則の取り方・省令のlawId検索がここにある |
| **`study_notes` の draft を書く** | `memory/feedback_studynotes_structure.md` を**開く**（この CLAUDE.md の括弧内の要約で代用しない）。追記先の `##` セクション**全体**と前後の `##` 見出しを Read する（原則5。grep のヒット行は確認にならない）|
| **当日のスコープ・最低ラインを宣言する** | `memory/feedback_session_planning.md` を開く。対象条文の**全項・枝番**を roadmap と照合し、外す部分の行き先を特定する（gapを作らない）|
| **一次確認で食い違い・誤りを見つけた** | 〔現行版・正しい内容の確定〕で止める。改正法の特定・施行日・全条差分・取得手順の発見は**その場でやらず** `study_log/宿題.md` へ送る |
| **宿題を消化した** | その事実が書かれている**すべてのファイル**を直す（`宿題.md`／`study_notes`／`laws.md`）。片方にしか反映しない型が2件実測（別表第二・不登法57条）|
| **スレッド長の警告（LEVEL1）が出た** | その日の区切りで終了処理を行い、次は新スレッド。**「終了処理か継続か」を聞き直さない**（決まっている＝鉄則D）|

**学習枠と保守枠**：**保守は枠を確保してから行う**（無枠で始めない。8/10＝業法1.5h枠のかなりが保守に流れ、37条の当てはめが1問しか進まなかった）。**枠をまたぐ材料は両方向とも、その場でやらず `宿題.md` に登録する**——〔学習枠の中で保守の材料を見つけた〕〔**保守枠の中で未収録の学習内容を見つけた**〕のどちらも同じ（後者＝8/16に177条の構造を直していて未収録2件が出た）。**振り分けの基準は分量ではなく作業の性質**＝〔構造・リンク・重複の是正＝保守枠で私が直す〕／〔収録内容そのものの追加＝学習枠でソクラテス式に通してから書く〕。**`study_notes` は「その日学んだこと」を書く場所なので、学んでいない内容を私が先に書き込まない。**

---

## 学習ログの運用（毎セッション必須）

セッションが終わったら以下を必ず実行する。**ユーザーが「終わる」と明示するまで開始しない**（鉄則D）。

1. **study_notes に追記** — その日学んだ条文・トピックを `study_notes/{科目}.md` へ。学習したのに未追記のまま終わらない。**追記前に draft（追記内容の全文）をユーザーに提示し、確認を受けてから書き込む**（時間がない・複数科目あるといった理由で省略しない。省略したい場合は省略してよいか先に確認する）。構造の5原則（条文番号順／関連法は末尾／相互リンク／実質的関連性／既存構造確認）は `memory/feedback_studynotes_structure.md`
2. **その日の想起対象を `study_log/宿題.md`「2の3 想起再テスト対象」に積む【必須】** — 落とした論点・理由がずれた論点に加え、**その日ブロックで通した範囲**（通読直後の正解は証拠にならないため）を、**日付・結果（○/×/△）・次回出題日**を追記して積む。**2回連続○で対象から外す**（1回で外すと exam_v6 の `lastWrong` と同じ「一度正解すると抜ける」欠陥を手作業で再現することになる）。**exam_v6.html への○×追加は任意**（PWAで外出先で回せるので残す。追加するなら `QUESTIONS` 配列へ・**正答と根拠条文は追加前に e-Gov 逐語確認**）。
   - **①ブロック直後＝「読解の確認」であって想起ではない**（2026-08-11 改定。素材は**その日取得した逐語に限る**・鉄則A原則14）。条文を貼った直後は**見ながらの照合**になるため定着を測れない（8/9実測＝建基法の4肢中3肢が同日中に反転／8/11＝保証の4肢5問全問正解だが条文表示中）。飛ばさないが、**結果を想起の証拠として扱わない**。／**②想起は必ず別セッション・条文非表示**で行い、`宿題.md` 2の3 の**「次回出題日」が来た項目だけ**を出す。**間隔は〔1日後・4日後・11日後〕**（×なら1日後へ戻す／○なら次の間隔へ／**2回連続○で外す**）。
   - **○×でなく4肢にする**（2026-08-09 改定）。同日に検出した3つ〔柱書の射程〕〔理由が条文とずれる〕〔**実在する数字を与えられると導出が起動しない**〕は**いずれも○×では再現できない**（○×なら正解して通過してしまう）。**根拠も一言書いてもらう**——「結論は合うが理由がずれる」型はそれでしか検出できない。
   - **exam_v6 の「間違いだけ」モードは母集団が localStorage にあり閲覧データ削除で消えるため、想起の母集団には使わない**（8/1に実際に消失し、8/9時点で誤答バンクは空＝8日間休眠していた）。**追加しただけで解かない問はバンクに入らない**。設計は `memory/exam_v6_redesign.md`。**exam_v6 の役割そのもの（○×専用でよいか）の見直しは R2 終了時＝8月末のレビューへ**
3. **`study_log/YYYY-MM-DD.md` を作成** — 雛形は `study_log/_template.md`。同じ日に複数回話したときは同じファイルに追記する
4. **`study_log/index.md` を更新** — 週次進捗テーブル／**当週の日別計画表の当日の行に「達成」**／科目別カバレッジ／セッション一覧（**先頭**に追加）。**「次回の学習予定」ブロックは2026-08-21に廃止**（次にやることは `宿題.md` 1 のトリガーへ書く）。細目は `memory/feedback_progress_metric.md`
5. **`study_log/宿題.md` を更新** — その日に消化した宿題は取り消し線＋消化日、新たに生じた未確認事項は追加。日次ログには宿題を書かず、このファイルに一本化する
6. **コミット＆プッシュ**

```bash
git add study_log/ study_notes/ exam_v6.html  # study_log/宿題.md も含まれる
# ↑に加え、触った回だけ： laws/ roadmap.html past_exams/ laws.md　　memory は対象外（リポジトリ外）
git commit -m "学習ログ：YYYY-MM-DD（学習内容の一言メモ）"
git push origin main
```

プッシュ後、スマホから https://makotize-dev.github.io/real-estate/study_log.html で確認できる。

---

## CLAUDE.md 保守ルール

- **上限目安**：150行／10KB。超えたら見直す。
- **詳細・背景・違反例は `memory/feedback_*.md` へ**。CLAUDE.md には鉄則の条文・手順名・必要最低限の操作的指示のみを残す。
- **各 Round 終了時にレビューし鋭く保つ**（R2 終了＝8月末が次のトリガー）。

---

## 開発ガイドライン

- **`main` で直接作業する**（ひとり学習のためレビュー・PRは不要。作業ブランチを作らない）。Claude Code を worktree で起動すると自動で `claude/` ブランチになるため、リポジトリ本体 `C:\dev\real-estate` で起動する。worktree で起動してしまった場合は、コミット後に `git push origin HEAD:main` で `main` へ反映する
- **コンテンツ**：税理士目線での解説を優先（既存の税務知識との比較・つながり）／暗記より理解／試験頻出事項は `【頻出】` と明示／表・フローチャートで視覚的に整理する
- **HTML**：スタンドアロン動作（サーバー不要・Tailwind CDN ＋ 埋め込み `<style>`）／スマホ・PC両対応のレスポンシブ／PWA対応（`manifest.json`・`sw.js` を全HTMLから参照）
